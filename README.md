# unraid-vim

Install vim as a plugin to unraid so it is usable on the command line.

## Installation

1. In Unraid, go to **Plugins** → **Install Plugin**.
2. Paste this URL in the "Install plugin from URL" field:
   ```
   https://raw.githubusercontent.com/mrrsm/unraid-vim/refs/heads/main/plugin/vim.plg
   ```
3. Click **Install**.